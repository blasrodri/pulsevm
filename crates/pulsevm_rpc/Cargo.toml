[package]
name = "pulsevm_rpc"
version = "0.1.0"
edition = "2024"

[dependencies]
pulsevm_abi = { path = "../pulsevm_abi" }
pulsevm_name = { path = "../pulsevm_name" }
serde_json = "1.0"
hex = "0.4"

[dev-dependencies]
serde_json = "1.0"
hex = "0.4"
